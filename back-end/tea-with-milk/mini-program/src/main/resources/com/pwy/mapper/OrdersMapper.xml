<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pwy.mapper.OrdersMapper">
 <select id="getOrderGoodsById" parameterType="string" resultType="OrdersGoods">
     select *from orders_goods where orders_id=#{id}
 </select>
</mapper>